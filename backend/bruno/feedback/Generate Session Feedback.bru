meta {
  name: Generate Session Feedback
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/feedback/generate/session/{{interviewId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
