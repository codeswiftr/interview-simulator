meta {
  name: Generate Response Feedback
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/feedback/generate/response/{{responseId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
