meta {
  name: Quick Practice
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/interviews/quick-practice
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  question_id: {{questionId}}
}
